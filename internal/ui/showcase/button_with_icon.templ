package showcase

import (
	"github.com/axzilla/templui/components"
	"github.com/axzilla/templui/icons"
)

templ ButtonWithIcon() {
	@components.Button(components.ButtonProps{
		Class: "flex gap-2 items-center",
	}) {
		@icons.Mail(icons.IconProps{Size: 16})
		Login with Email
	}
}
