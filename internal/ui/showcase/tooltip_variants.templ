package showcase

import "github.com/axzilla/templui/components"

templ TooltipVariants() {
	<div class="flex space-x-4">
		@components.Tooltip(components.TooltipProps{}) {
			@components.TooltipTrigger(components.TooltipTriggerProps{}) {
				@components.Button(components.ButtonProps{
					Variant: components.ButtonVariantOutline,
				}) {
					Default
				}
			}
			@components.TooltipContent(components.TooltipContentProps{
				Side:    components.TooltipTop,
				Variant: components.TooltipDefault,
			}) {
				Default tooltip
			}
		}
		@components.Tooltip(components.TooltipProps{}) {
			@components.TooltipTrigger(components.TooltipTriggerProps{}) {
				@components.Button(components.ButtonProps{
					Variant: components.ButtonVariantOutline,
				}) {
					Secondary
				}
			}
			@components.TooltipContent(components.TooltipContentProps{
				Side:    components.TooltipTop,
				Variant: components.TooltipSecondary,
			}) {
				Secondary tooltip
			}
		}
		@components.Tooltip(components.TooltipProps{}) {
			@components.TooltipTrigger(components.TooltipTriggerProps{}) {
				@components.Button(components.ButtonProps{
					Variant: components.ButtonVariantOutline,
				}) {
					Destructive
				}
			}
			@components.TooltipContent(components.TooltipContentProps{
				Side:    components.TooltipTop,
				Variant: components.TooltipDestructive,
			}) {
				Destructive tooltip
			}
		}
	</div>
}
